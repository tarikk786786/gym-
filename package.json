{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) if [ -z \"$CI\" ]; then echo \"Use pnpm instead\" >&2; exit 1; fi ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "netlify:build": "pnpm --filter @workspace/gym-planner run build"
  },
  "private": true,
  "dependencies": {
    "@noble/ciphers": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "@replit/connectors-sdk": "^0.4.1",
    "fontkit": "^2.0.4",
    "js-md5": "^0.8.3",
    "linebreak": "^1.1.0",
    "pdfkit": "^0.19.1",
    "png-js": "^2.0.0",
    "restructure": "^3.0.2",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  }
}
